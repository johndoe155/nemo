/**
 * Static assets served from Vite's public directory.
 * BASE_URL keeps the reference working for both root and sub-path builds.
 */
export const LOGO_SRC = `${import.meta.env.BASE_URL}nemo.svg`;
